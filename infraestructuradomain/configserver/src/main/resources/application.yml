spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CAVences/paymentchainparent-config.git
          default-label: main
          clone-on-start: true
  security:
    user:
      name: user
      password: qwerty

server:
  port: 8888